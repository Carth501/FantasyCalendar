export interface CalendarSettings {
    startingDayID: number;
    daysPerYear: number;
    daysPerMonths: number[];
    startingDoW: number;
    DoW_names: string[];
    monthNames: string[];
    currentYear: number;
}

